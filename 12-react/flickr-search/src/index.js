import React, { Component } from 'react'; // its actually preact
import ReactDOM from 'react-dom'; // its really preact
import 'preact/devtools';

import App from './components/App';

ReactDOM.render(<App />, document.body);
